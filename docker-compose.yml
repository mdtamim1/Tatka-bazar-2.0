
services:
  postgres:
    image: postgres:16-alpine
    container_name: tatka_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: tatka
      POSTGRES_PASSWORD: tatka_secret
      POSTGRES_DB: tatka_bazar
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tatka -d tatka_bazar"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: tatka_redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
