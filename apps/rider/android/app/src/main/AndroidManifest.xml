<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--
            Tatka Rider — High-Priority Notification Channel
            Orders will ring even when the phone is on silent mode (IMPORTANCE_MAX)
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="tatka_orders" />

    </application>

    <!-- ─── Core Network ─────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ─── GPS / Location ───────────────────────────────────────── -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
        ACCESS_BACKGROUND_LOCATION: Required so GPS tracking continues
        when the rider locks the screen or switches apps.
        Android 10+ (API 29+) requires this declared separately.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ─── Foreground Service (Background GPS Tracking) ─────────── -->
    <!--
        FOREGROUND_SERVICE: Keeps the GPS-broadcast service alive
        with a persistent "Tatka Rider is tracking your location" notification.
        FOREGROUND_SERVICE_LOCATION: Required on Android 14+ (API 34+).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- ─── Screen Wake Lock ─────────────────────────────────────── -->
    <!--
        WAKE_LOCK: Prevents the CPU from sleeping while the rider's
        navigation map is active — matches Web Screen Wake Lock API.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ─── Haptic Feedback ──────────────────────────────────────── -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ─── Push Notifications (Android 13+, API 33+) ───────────── -->
    <!--
        POST_NOTIFICATIONS: Required on Android 13+ for runtime
        notification permission. tatka_orders channel uses IMPORTANCE_MAX
        so orders ring even in Do Not Disturb mode.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ─── Boot Receiver (Auto-restart GPS service after reboot) ── -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

</manifest>
